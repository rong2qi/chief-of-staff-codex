interface:
  display_name: "Context Handoff"
  short_description: "Preserve long Codex tasks across context rollover"
  default_prompt: "Use $context-handoff to checkpoint and safely continue this long-running task."
policy:
  allow_implicit_invocation: true

